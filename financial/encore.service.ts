import { Service } from "encore.dev/service";

// The financial service manages Chart of Accounts, double-entry journal entries,
// ledger lines, bank client placements, and partner disbursement calculations.
export default new Service("financial");
